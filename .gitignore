# === 시크릿 (절대 커밋 금지) ===
api key.txt

# === 대용량 원본 데이터 (GitHub 100MB 초과 / 저장소 비대상) ===
*.zip
!HWASEONG_ADM_DONG.zip
*.csv
GIS건물통합정보.zip
토지특성정보.zip
부동산개발업공간정보.zip
배포패키지_vercel_supabase.zip
배포패키지_vercel_supabase/

# === 원본 hwpx / 이미지 산출물 (로컬 작업물, 저장소 비대상) ===
*.hwpx
*.png
*.jpg

# === 작업 인수인계본 — 다른 PC 에서 이어 가려면 있어야 한다.
#     실제 키는 담기지 않는다(키 '이름'만 적는다). 그래서 올린다.
# CLAUDE.md

# === 개인 Claude Code 설정 (git 미추적) ===
.claude/settings.local.json

# === D-02 코드 분리로 떼어낸 앱 자산 (인라인 base64 대체 — 반드시 커밋) ===
!data/icons/*.png

# === 검증용 임시 스크립트 (커밋 대상 아님) ===
_smoke.mjs
_*.mjs

# 원본 shp 묶음 — 다른 PC 에서 다시 변환할 수 있게 함께 둔다(752KB)
# HWASEONG_ADM_DONG.zip
_shp/

# === 검증용 로컬 패키지(jsdom) — 커밋 대상 아님 ===
node_modules/

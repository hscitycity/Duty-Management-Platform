# 상황판_사양서.md (v3)

당직자 상황판 재구축 사양 — 도와드림QR

> **v1 실패를 반영한 개정판이다.** v1은 "구획별 고정 높이"를 정했으나 담을 내용이 그 높이에 맞지 않아
> 모든 요소가 납작해지고 스크롤바가 다섯 개 생겼다. 이번에는 **화면을 쪼개서** 각 화면이 한 가지 일만 하게 한다.
> 함께 볼 것: `기동단_사양서.md` · `행정조직도.md` · `design_console.md`

---

## 0. 이번 개정의 원칙

**① 한 화면은 한 가지 일만 한다.**
v1의 상황판은 목록·지도·상세·접수·일지·인력·뉴스·타임라인을 전부 담았다. 그래서 접수 입력창이 납작해지고 일지는 열리지도 않았다. 접수와 일지는 **별도 화면으로 뺀다.**

**② 내용이 안 들어가면 높이를 줄이지 말고 항목을 뺀다.**
v1이 무너진 지점이다. 고정 높이에 내용을 욱여넣지 않는다. 6장의 「담지 않는 것」을 지킨다.

**③ 스크롤 영역은 화면당 최대 2개.**
현재 다섯 개다. 좌측 목록 하나, 우측 상세 하나. 그 외 구획은 스크롤 없이 전부 보이거나, 안 보이면 항목을 줄인다.

**④ 글자 크기는 6단계만.**
12 · 13 · 15 · 17 · 22 · 30px. 그 외 금지. 요소별 지정은 8장.

**⑤ 라벨 없는 숫자·버튼을 만들지 않는다.**
`1시간 6시간 24시간`이 무엇인지, `흐림 11건`이 무엇인지 화면만 보고 알 수 있어야 한다.

---

## 1. 화면 목록

| 주소 | 탭 이름 | 하는 일 |
|---|---|---|
| `#duty` | **민원현황-접수** | 열린 민원을 지도·목록으로 보고, 접수·조치까지 여기서 한다 |
| `#info` | **일반현황** | 인력·뉴스·기상·지표·부서현황 |
| `#dash` | **처리결과** | 누적 처리 데이터 분석 |
| `#road` | **교통·도로 기동단** | 별도 사양서 |
| `#cmd` | 조치지시 | 헤더 우측 별도 버튼. 급증 탐지 시 |
| `#cit` `#track` | 24시간 민원접수창구 · 처리현황 | 헤더 우측 노란 버튼. 시민용 |
| `#done` | 조치결과 보고 | 용역업체 전용. 문자 링크로만 접근(7장) |

**v2에서 계획했던 `#new`(접수)·`#log`(일지) 별도 화면은 취소한다.**
접수와 일지는 **민원현황 우측 패널 안에서** 처리한다(6장). 화면 수를 늘리지 않는 편이 정리에 유리하다.

**일반현황도 우측 패널에 표시한다.** 좌측 목록과 지도는 그대로 두고 우측만 바뀐다.

---

## 2. 상황판 골격 (1440×900 기준)

```
┌──────────────────────────────────────────────────────────────┐
│ 헤더 48px  (모든 화면 공통 부품)                                 │
├─◀─────────────┬──────────────────────────┬──────────────▶───┤
│ 좌측 320       │          지도             │ 우측 340         │
│ (접기 가능)     │       (잔여 전체)          │ (접기 가능)       │
│ 분류 카드 3    │  도구는 지도 위 오버레이     │ 선택 시 640      │
│ + 민원 목록    │                          │ 4가지 상태(6장)   │
│    852px      │         852px            │      852px       │
└───────────────┴──────────────────────────┴──────────────────┘
```

높이: `900 − 48(헤더) = 852px`. **하단 스트립은 없앤다.** v2에서 계획했던 인력·뉴스·지표는 일반현황(`#info`)으로 옮긴다. 좁은 칸에 넣어 판독이 안 되던 문제를 자리를 옮겨 해결한다.

### 2.1 사이드바 접기

| 번호 | 대상 | 동작 |
|---|---|---|
| **E-1** | 좌측 | 경계 한가운데 **손잡이(16×44px 알약)** 를 누르면 접힘 |
| **E-2** | 우측 | 같은 모양의 손잡이. 동일 |

- **패널만 접힌다. 지도는 그 자리에서 넓어질 뿐 사라지지 않는다.**
  격자 다섯 칸의 자리를 `grid-column` 으로 못 박고, 접을 때는 폭만 0 으로 만든다.
  `display:none` 으로 없애면 격자가 남은 것들을 앞에서부터 다시 채워
  지도가 5px 짜리 경계 칸으로 밀려난다(실제로 좌측을 접을 때 그렇게 깨졌다).
  접힘 상태에서도 경계 14px 를 남겨 손잡이가 계속 보이게 한다(별도 세로 탭을 만들지 않는다).
- 손잡이는 접기 전용이고 경계는 폭 조절 전용이다. 손잡이를 끌어도 폭이 변하지 않는다.

- 접힘 상태를 저장해 다음 접속 시 유지한다.
- 좌측을 접어도 지도 마커 클릭으로 민원을 선택할 수 있어야 한다.

### 2.2 폭 조절

- 좌측·지도, 지도·우측 **경계를 마우스로 끌어 폭을 조절**한다.
- 범위: 좌측 260~480px, 우측 300~720px. **지도는 최소 480px 유지**하며 그보다 좁아지는 드래그는 멈춘다.
- 조절값을 저장한다. 경계 우클릭 메뉴에 초기화를 둔다.
- 드래그를 놓는 순간 `map.invalidateSize()`를 호출해 타일 깨짐을 막는다.

---

## 3. 헤더 (48px, 1줄)

```
[로고]  민원현황  일반현황  처리결과  교통·도로 기동단 │ 🔍 민원검색 │ 표시: 일반 · 긴급 · 교통·도로 기동단 │ ● LIVE │ 10:14:29 │ 👤 소속 │ [조치지시] [24시간 민원접수창구]
```

### 3.1 화면 이름 통일

| 주소 | 기존 이름 | **바꿀 이름** |
|---|---|---|
| `#duty` | 당직자 상황판 | **민원현황-접수** |
| `#dash` | 종합현황 대시보드 | **처리결과** |
| `#road` | 신속기동단 작업판 | **교통·도로 기동단** |
| `#info` | (신설) | 일반현황 |

화면·문서·시나리오 전부에서 이 이름으로 교체한다.
※ 기동단은 정식 명칭이 「교통·도로민원 신속처리 기동단」이므로 축약형에 가운뎃점을 유지한다(`기동단_사양서.md` 0장). 뺄지 여부는 확인 후 결정.

### 3.2 좌측 탭 (4개)

`민원현황-접수 · 일반현황 · 처리결과 · 교통·도로 기동단`

### 3.2-1 헤더는 공통 부품으로 만든다 【중요】

현재 화면마다 헤더를 따로 그려서 민원현황만 새 헤더이고 나머지는 옛 헤더(로고+부제+옛 탭명+조직도 줄+당직자 줄)가 남아 있다.

- **헤더를 한 곳에서 그리는 함수**(`renderHeader` 등)로 만들고 모든 화면이 호출한다. 화면별 개별 헤더 마크업을 전부 제거한다.
- 화면별 차이는 **인자로만** 처리한다.
  - 시민·업체 화면(`#cit` `#track` `#done`) — 탭·필터·소속·조치지시 **숨김**. 로고와 화면명만. 내부 정보를 외부에 노출하지 않는다.
  - 그 외 — 전체 표시
- **제거**: 로고 옆 부제(`재난안전상황실(5층)…`), 조직도 줄, 당직자 별도 줄, 옛 탭 이름
- 화면 전환은 헤더 탭으로만. 화면 안에 또 다른 전환 버튼을 두지 않는다.

### 3.3 우측 별도 버튼 (2개)

탭과 성격이 다르므로 **오른쪽 끝에 따로 뺀다.**

| 버튼 | 색 | 이유 |
|---|---|---|
| **24시간 민원접수창구** | **밝은 노란색** (배경 `#FFD84D`, 글자 `#3A2E00`) | 시민이 보는 화면. 내부 업무 탭과 성격이 다르다 |
| **조치지시** | 적색 계열 (`#D63B1F` 테두리, 급증 시 채움) | 상황실 권한 전용. 급증 탐지 시에만 활성 |

- 두 버튼은 탭 묶음과 **세로 구분선으로 분리**한다.
- 조치지시는 상황실 소속이 아니면 흐리게 표시하고 눌러도 안내만 뜬다.
- 급증 탐지 중이면 조치지시 버튼에 건수 배지 + 점멸(최고 심각도에만).
- 24시간 민원접수창구는 **새 창으로** 연다(당직 화면을 잃지 않게).

### 3.4 검색·필터

| 요소 | 규칙 |
|---|---|
| **민원검색** | 문구를 `🔍 민원검색`으로. `Ctrl K`는 툴팁·단축키 안내로만 표시하고 버튼 문구로 쓰지 않는다 |
| ~~표시 필터~~ | **제거.** 좌측 분류 카드(C-2)와 같은 축이다. 조작기를 두 군데 두면 어느 쪽이 참인지 알 수 없다 |
| **범위(1시간·6시간·24시간)** | **제거.** 열린 민원을 다루는 화면이므로 시간 범위를 나눌 이유가 없다 |
| 제거 대상 | 기존 `전체 · 긴급 · 미처리` 필터(분류와 축이 겹친다), `필터` `범위` 라벨 |

- **분류를 고르는 곳은 좌측 카드 하나뿐이다.** 헤더에 같은 것을 또 두지 않는다.
- 필터 적용 시 지도 마커는 숨기지 않고 흐리게. 그 아래 `필터 제외 N건 — 흐리게 표시 [전체 보기]`.

### 3.5 나머지

- 시계·소속은 각 1개. 조직도 줄·중복 표시 제거.
- 소속 선택은 3단계(상황실 / 부서 / 행정복지센터). 목록 출처는 `행정조직도.md`이며, 분장사무 데이터로 목록을 만들지 않는다.

---

## 4. 좌측 — 민원 목록 (320px)

### 4.1 상단 — 분류 카드 3개

목록을 한 덩어리에 쌓지 않는다. **입구를 셋으로 나누고 하나씩 본다.**

```
┌──────────┐ ┌──────────┐ ┌──────────┐
│  긴급    │ │  일반    │ │ 교통·도로 │
│    8     │ │   19     │ │   12     │
└──────────┘ └──────────┘ └──────────┘
```

| 카드 | 담는 것 | 색 |
|---|---|---|
| **긴급민원** | 긴급도 = 긴급 | 적색 `#D63B1F` |
| **일반민원** | 긴급도 = 일반 | 파랑 `#1D4994` |
| **교통·도로 기동단** | 기동단 이관 건 | 주황 `#F07B27` |

**긴급과 기동단은 축이 다르다.**
- `긴급` = 우선순위 · `일반` = 우선순위 · `교통·도로 기동단` = **담당 주체**
- 도로 함몰처럼 **긴급이면서 기동단 건**일 수 있다. 이때는 **두 카드에 모두 집계·표시**한다.
- 카드를 상호배타로 만들지 않는다. 긴급 카드에서는 `기동단` 배지가, 기동단 카드에서는 `긴급` 배지가 붙는다.
- 건수 합계가 전체 건수와 다를 수 있다. 하단에 `※ 긴급·기동단 중복 집계` 안내 한 줄.

- 카드 높이 64px, 3등분. 라벨 12px + 건수 22px.
- **그라데이션 입체 버튼**으로 한다(조치 3버튼 C-4 와 같은 규칙).
  긴급 적 · 일반 청 · 교통·도로 주. 누르면 눌리는 그림자.
- **고른 카드만 제 색을 내고 나머지는 채도를 낮춘다.** 지금 무엇을 보고 있는지가 색으로 드러난다.
- 선택된 카드는 진한 배경 + 흰 글자. 나머지는 흰 배경 + 색 테두리.
- **한 번에 하나만 선택된다.** 아래 목록이 그 분류로 바뀐다.
- 기본 선택은 **긴급민원**. 건수가 0이면 일반민원으로.
- 건수는 실시간 갱신. 새 건이 들어오면 해당 카드에 잠깐 강조 표시.
- **교통·도로 기동단 카드는 건수만 보여주고, 클릭 시 목록을 띄운다.**
  상세 처리는 `#road` 화면에서 한다(목록 하단에 `기동단 작업판 열기` 링크).

### 4.1-1 제보 신빙성 — 확인중 / 처리대상

한 건만 들어온 제보는 오신고·허위신고일 수 있다.
**같은 곳에서 같은 종류의 제보가 여러 건 모이면 그때 신빙성이 선다**(티맵 소셜 제보와 같은 방식).

분류 카드(긴급·일반·교통·도로) **바로 아래 하위 버튼 2개**로 둔다.

| 번호 | 버튼 | 담는 것 |
|---|---|---|
| **C-2d** | 🔎 민원제보 확인중 | 유사제보가 아직 기준에 못 미치는 건 |
| **C-2e** | ✅ 처리대상 민원목록 | 동일지역 유사제보가 기준 이상인 건 · **기본값** |

**기본은 처리대상이다.** 신빙성이 선 건부터 손대는 것이 순서다.
확인중은 눌러야 보인다 — 아직 오신고일 수 있는 건이다. 다시 누르면 전체.

- **기준을 새로 만들지 않는다.** 시행 중인 탐지 규칙(⚙️)의 `반경 · 시간창 · 최소 건수`를
  그대로 쓴다(기본 1,000m · 3시간 · 3건). 규칙을 고치면 이 화면도 함께 움직인다.
- 같은 세부유형 · 반경 안 · 시간창 안의 제보를 자기 자신을 포함해 센다.
- 카드에 `유사 N` 배지를 달고, 기준을 넘으면 초록으로 바꾼다.
- 고르면 목록과 **지도 핀이 함께** 좁혀지고, 무엇을 보는 중인지 한 줄로 알린다.
  그 줄에서 [기준 보기]로 탐지 규칙 화면에 갈 수 있다.
- 다시 누르면 전체로 돌아온다. 상세의 담당부서 구획·배정 근거 팝업에도 같은 값을 적는다.

### 4.2 그 아래 한 줄 (24px)

```
미처리 19 · 출동중 2 · 오늘완료 11               [CSV]
```
- 숫자 15px, 라벨 12px. 카드·박스로 만들지 않는다.
- 필터가 걸려 있으면 12px 한 줄 추가: `필터 제외 11건 — 흐리게 표시  [전체 보기]`
- **제거**: `시장 · 20건 (우리 0 · 타 부서 20)` → 소속 버튼 툴팁으로

### 4.3 목록 (약 620px, 스크롤 1개)

선택한 분류의 민원만 최신순으로 나열한다. **구획 머리글이 없다** — 이미 분류가 정해져 있으므로.

**카드 = 52px 고정, 2줄.**
```
10:05  낙하 위험물 · 안전재난        📷 신규
효행구 봉담 일원 · #13675
```
- 1줄: 시각 12px mono · 유형 13px · 아이콘 · 상태 배지(최대 2개)
- 2줄: 위치·번호 12px 회색
- **내용 요약 문장을 카드에 넣지 않는다.** 우측 상세에만 둔다.
- 넘치면 말줄임. 줄바꿈 금지.
- 좌측 3px 상태색 바: 신규접수 / 처리중 / 인계됨
- 처리구분(즉시·단기·중장기)은 **교통·도로 분류에서만** 배지로 표시한다.

**표시 개수**: 620px에서 카드 **10개 이상**이 보여야 한다.

**목록이 비면**: `해당 분류에 열린 민원이 없습니다`를 회색으로. 빈 화면으로 두지 않는다.

### 4.4 지도와의 연동

- 분류를 바꾸면 **지도 마커도 그 분류만 진하게** 표시한다. 나머지는 흐리게(숨기지 않는다).
- 목록에서 민원을 고르면 지도가 그 위치로 이동하고 우측 상세가 열린다.

---

## 4-1. 24시간 민원접수창구 흐름 (`#cit`)

**위치를 먼저 찍는다.** 어디인지 모르면 무엇을 접수해도 처리할 수 없다.

| 번호 | 단계 | 내용 |
|---|---|---|
| **A-2** | 위치 | 화면에 들어오면 현재 위치를 **자동으로** 한 번 잡는다. 지도 이동·주소 검색도 가능 |
| **A-4** | 대분류 | **생활민원 / 교통·도로민원 두 개뿐.** 교통·도로는 기동단이 바로 받는다 |
| **A-5** | 세부분류 | 생활 → `FIELDS`(교통·도로 제외) · 교통·도로 → `ROAD_CATS` 5종 |
| **A-7** | 확인 | 추가 설명 · 사진 |

### 위치 권한 (A-3)

- **권한이 켜져 있으면 안내를 띄우지 않는다.** 안내부터 뜨면 사용자는 오류로 받아들인다.
- `permissions.query` 가 없거나 답을 못 주면 **안내 없이 바로 물어본다.**
- 실패해도 한 번에 포기하지 않는다: 고정밀(9초) → 저정밀(12초) → 그래도 안 되면 지도 지정 안내.
- 사용자가 설정에서 권한을 켜면 `onchange` 로 **자동 재시도**한다.

---

## 5. 지도

### 5.1 도구

| 위치 | 내용 | 상한 |
|---|---|---|
| 좌상단 칩 | 민원·**행정동**·행사인파·CCTV·돌발상황·지적 + `전체` | 한 줄 6개 |
| 우상단 세로 | 항공 · 기상 · 야간 · 탐색 | **6개 상한**, 32px 알약, 아이콘 + 라벨 |
| 좌상단(G-6) | 내위치 | 확대·축소 바로 아래 |
| 우하단(G-7) | 전체화면 | |

- **기상 관제(Windy)를 켜면**(G-9) Windy 판이 **지도 자리를 대신한다.**
  Windy 판은 지도 내용이므로 도구(400)보다 아래(z 2)에 깔고 Leaflet 지도는 숨긴다.
  오버레이 패널로 덮으면 [레이어]를 눌러도 아무것도 안 보인다 — 실제로 그랬다.
- Windy 도구 줄은 좁은 화면에서 두 줄로 접힌다. 우리 레이어 칩은 **[☰ 레이어] 하나로 묶고**
  레이어바·패널을 **110px 아래로** 내린다.
- **우상단 도구도 [☰ 지도도구] 하나로 묶어 좌측 내위치 아래로 옮긴다.**
  눌렀을 때만 펼쳐진다. Windy 화면을 최대한 비워 둔다.
- **좌상단 확대·축소는 레이어 칩 줄 아래(44px)에 둔다.** 겹치면 눌리지 않는다.
- **기상 중에는 지도 클릭이 접수로 이어지지 않는다.** 보는 화면이지 접수하는 화면이 아니다
  (전에는 Windy 버튼을 누를 때마다 「화성시 관할지역이 아닙니다」가 떴다).
- **내위치는 Windy 화면을 옮긴다.** 뒤에 숨은 Leaflet 지도를 옮겨도 보이지 않는다.
  보던 기상 항목(비 레이더 등)은 그대로 유지한다.
| 좌하단 | `23°C 맑음 · PM10 좋음` 배지 | 1개 |
| 우하단 | 축척 | — |

- `전체` → 레이어 목록이 **지도 위 오버레이 패널**로 (좌측을 차지하지 않는다)
- **우상단 도구는 알약 모양**으로 한다. 높이 32px · 좌우 여백 12px · 폭 auto · 아이콘 + 라벨(`🛰 항공`).
- **여섯 개를 넘기지 않는다.** 일곱 번째가 필요하면 무엇을 뺄지 함께 정한다(§10.1 규칙 5).
  `🛵 따라가기`·`🏷️ 설명 라벨`은 표시 옵션이므로 **레이어 오버레이 패널**로 옮겼다.
- **같은 일을 하는 버튼을 Leaflet 컨트롤로 또 만들지 않는다.** `id` 가 둘이 되어 상태 표시가 어긋난다.
  (실제로 `panBtn` 이 도구 묶음과 지도 컨트롤에 각각 있어 한쪽이 늘 꺼진 것처럼 보였다.)
- **민원 레이어를 꺼도 선택한 민원 하나는 지도에 표시한다.** 목록에서 고른 건이 지도에서 사라지면 위치를 확인할 수 없다. 선택 마커는 레이어 토글의 영향을 받지 않는다.
- 토글: 켜짐 = 진한 배경 + 흰 글자 / 꺼짐 = 흰 배경 + 회색. **현재 반대다.**
- **행정동 경계(G-8)는 기본으로 켜 둔다.** 민원 위치가 어느 동인지 지도에서 바로 읽혀야 한다.
  얇은 실선(굵기 1.2 · 투명도 .55) + 채움 3% + 클릭 통과 + 맨 뒤 배치 — 민원 마커를 가리지 않는다.
  출처는 통계청 행정동 경계(BND_ADM_DONG_PG). shp 는 `scripts/shp2geojson.mjs` 로
  EPSG:5186 → WGS84 변환하고 약 12m 허용오차로 간략화한다(점 88% 감축 · 179KB).
- 마커 색 = 긴급도·상태만. 부서·관할구를 색으로 나누지 않는다.
- **마커 아이콘이 지금 과하게 크고 장식적이다. 28px 이내 단순한 형태로.**

### 5.2 출동 인력 표현

- 항상 적용: 부드러운 이동(보간) · 진행 방향 회전 · 지나온 길 흐리게 · 도착 임박 표시(잔여 18%)
- **마커를 지웠다 다시 만들지 않는다.** id 별로 살려 두고 `setLatLng` 으로 자리만 옮긴다.
  새로 만든 요소에는 전환이 걸리지 않아 툭툭 끊겨 보인다. 갱신은 `requestAnimationFrame`(상황판에서만).
- **지나온 선의 끝점은 지금 위치다.** 경로 꼭짓점까지만 그리면 선이 마커보다 뒤처진다.
  선도 마커와 같은 프레임에 다시 그린다.
- **좌표를 뭉개지 않는다.** `coarse()`(100m 격자)는 **시민 화면 전용**이다.
  내부 화면에서 그대로 쓰면 마커가 격자 단위로 순간이동한다. `trackPublic(id, true)` 로 정밀 좌표를 쓴다.
- 선택: `따라가기` 토글 — 상황판 기본 꺼짐, 지도 조작 시 자동 해제 + `다시 따라가기`
- 자동: 위치 갱신 끊기면 마커 흐림 + `위치 확인 중` (토글 아님)
- 라벨: `향남 순찰조 · 2.4km` — 조직 단위. 개인 이름·연락처 금지
- 도착 예정: **시각으로**(`21:31`), 1분 미만은 `곧 도착`. OSRM 응답의 소요시간 사용
- 시민 화면 진행 막대와 **같은 값**을 쓴다

---

## 6. 우측 패널 — 4가지 상태

우측은 상황에 따라 네 가지로 바뀐다. **한 번에 하나만.** 여러 일을 한 화면에 겹치지 않는다.

| 상태 | 폭 | 전환 |
|---|---|---|
| **① 기본** | 340px | 아무것도 선택하지 않은 상태 |
| **② 민원 상세** | 640px | 목록·마커에서 민원 선택 |
| **③ 접수 입력** | 640px | 접수 버튼 클릭 |
| **④ 일반현황** | 640px | 헤더 `일반현황` 탭 |
(기동단 안내는 우측 패널이 아니라 `#road` 화면 안에서 펼친다 — 6.5 아래 참고)

- 상세를 보다 접수를 누르면 상세는 닫힌다. 반대도 같다.
- **기동단 안내는 우측 패널에 두지 않는다.** 기동단 화면(`#road`) 안에서 펼친다 — 그 화면을 보는 사람에게 필요한 글이다.
- 확장 시 지도는 줄되 **최소 480px 유지**.
- 1280px 미만에서는 확장 대신 전체 폭 오버레이 + 백드롭.

### 6.1 ① 기본 — 접수 입구 3개

```
목록이나 지도에서 민원을 고르세요.

┌───────────────────────┐
│  📝 일반민원 접수       │  당직자·타 부서 직원
├───────────────────────┤
│  🚧 교통·도로 기동단 접수 │  기동단 전담 인력
├───────────────────────┤
│  📕 재난상황일지 작성    │  재난안전상황실 직원
└───────────────────────┘

최근 처리
09:16  하수도 막힘 · 향남읍
```

**이 세 입구로 화성시 민원이 전부 커버된다.** 버튼 높이 56px, 라벨 15px + 대상 12px.
**그라데이션 입체 버튼**으로 한다(조치 3버튼 C-4 와 같은 규칙) — 일반민원 청 · 기동단 주 · 일지 보라.
**최근 처리는 10건까지만** 보인다. 그 이전은 처리결과 화면에서 본다.

### 6.2 ② 민원 상세

민원을 고르면 640px로 펼쳐진다. **모든 구획은 펼침이 기본이다.** 토글을 눌러야 보이게 하지 않는다.

**상단 — 조치 4구획** (항상 보이는 위치에 고정)

| 구획 | 내용 |
|---|---|
| **현장확인** | 확인자·시각·판정, 인력 투입 |
| **용역업체 문자발송** | 업체 선택·발송·수신 확인, 조치결과 링크 동봉(7장) |
| **조치결과** | 전/후 사진, 결과 구분. **업체가 외부에서 입력한 것도 여기 표시** |
| **민원회신** | 신고인 통보 내용·시각·방법 |

각 구획은 진행 상태를 색으로 표시한다(대기 회색 / 진행 주황 / 완료 파랑).

**하단 — 세부 6구획** (이 순서 고정)

| 순서 | 구획 | 내용 |
|---|---|---|
| 1 | **민원내용** | 위치 · 문제상황 · 조치필요사항 |
| 2 | **담당부서** | AI 분석 결과 · 배정 근거 · 확신도. **처리주체 변경은 부서 목록에서 선택**(직접 입력 금지, 행정조직도 기반) |
| 3 | **담당연락처** | 부서·업체 연락처. 같은 문장을 제목·본문에 반복하지 않는다 |
| 4 | **현장사진** | 신고 / 조치 전 / 조치 후 |
| 5 | **대응매뉴얼** | 처리 순서 |
| 6 | **처리이력** | 시각이 붙은 타임라인 |

- **색을 통일한다.** 구획 머리글은 같은 크기·같은 색, 강조는 상태색만. 구획마다 다른 배경색·아이콘 색을 쓰지 않는다.
- 상세는 **한 덩어리로 스크롤**한다. 구획별 개별 스크롤 금지.
- 조치 3버튼은 가로 3열, 그 아래 세부 6구획.
- **구획은 제목과 내용을 한 덩어리(`section`)로 묶는다.** 둘을 형제로 나열하면
  여러 열로 배치할 때 제목과 내용이 서로 다른 칸으로 흩어져 카드가 반쪽으로 보인다.
- **열 수는 우측 패널의 실제 폭을 따른다**(`auto-fit, minmax(260px, 1fr)`).
  폭이 드래그로 300~720px 를 오가므로 고정 열 수로는 맞지 않는다.

### 6.3 ③ 접수 입력

접수 버튼을 누르면 640px로 펼쳐지고 **지도가 위치 지정 모드로 바뀐다.**

- 지도를 클릭하면 그 지점이 민원 위치가 된다. 주소는 역지오코딩으로 자동 채운다.
- **주소를 직접 입력하지 않아도 된다.** 교통·도로 민원도 도로 위를 클릭하면 끝난다.
- 지정 후 핀을 끌어 미세 조정 가능. **안내 띠는 지도 아래쪽**에 둔다(G-5).
  위쪽은 레이어 칩 자리다 — 상단에 두면 겹친다. 좌하단 기상 배지·우하단 전체화면과도
  겹치지 않게 한 줄 위(bottom 52px)에 놓고, 좁은 지도에서는 줄바꿈한다.

**일반민원 접수** — 당직자·타 부서 직원
```
자유 기록 (AI 서기)  입력창 160px 이상
[AI로 정리하기]
정리 결과 — 유형·긴급도·위치·요약 (수정 가능)
담당  ( ) 당직 처리   ( ) 교통·도로 기동단     ← AI 제안 + 근거
[접수 등록]
```

**교통·도로 기동단 접수** — 전담 인력이 전화받으며 이어서 기록
```
통화 기록  ← 계속 이어 쓰는 큰 입력창
5개 유형 버튼 (도로파손·교통시설·불법주정차·버스불편·교통체계)
위치: 지도 클릭
접수 즉시 [안내 마감 10:56] 표시     ← 접수+60분, 기동단 사양서 5.1
[접수 등록]
```

**재난상황일지 작성** — 재난안전상황실 직원
```
자유 서술 → parseJournal 정리 결과 (수정 가능)
[일일보고 생성] [민원접수서 생성]
과거 일지 목록
```

- 등록 후 **배정된 주체의 화면으로** 이동하고 해당 건이 선택된 상태로 열린다.

### 6.4 ④ 일반현황

헤더 `일반현황` 탭을 누르면 우측에 표시된다. 좌측 목록과 지도는 그대로.

| 번호 | 구획 | 표시 방법 |
|---|---|---|
| **F-1** | 기상·대기 | **맨 위에 늘 펼쳐 둔다** |
| **F-2** | 인력 현황 | **직사각형 버튼 → 팝업.** 버튼에는 한 줄 요약만 |
| **F-3** | 부서현황 | **직사각형 버튼 → 팝업.** 나열식이 아니라 **조직도**로 그린다(아래) |
| **F-4** | 실시간 뉴스 | 시각 + 제목 + 출처. **한 쪽 20건씩 넘겨 보고, 과거 기사도 불러온다** |

#### F-3 부서현황은 조직도로 그린다

쭉 나열하면 어느 국 소속인지 알 수 없다. **시장 → 부시장 → 실·국 → 과** 계층을 그대로 그린다.

```
                 시장
         (시장 직속: 공보실)
   ┌───────────────┴───────────────┐
제1부시장                      제2부시장
 실·국 카드 + 사업소            실·국 카드 + 사업소
   └ 과를 칩으로

구청 4 · 행정복지센터 29 (구별로 묶음)
```

- 카드 왼쪽 색 띠로 갈래를 구분한다(직속 보라 · 제1부시장 파랑 · 제2부시장 회청 · 사업소 초록 · 구청 주황 · 행정복지센터 회색).
- **검색은 계층을 무너뜨리지 않는다.** 걸린 과만 노랗게 강조하고, 걸리지 않은 카드는 뺀다.
- 과가 없는 기관(예: 대외협력사무소)도 자리를 남긴다 — 조직도에 있는 기관이다.
- 좁은 칸(일반현황 패널)에는 그리지 않는다. 계층이 보이지 않기 때문이다.

#### F-4 뉴스는 넘겨 본다

- **한 쪽 20건.** 100건을 한 화면에 쌓으면 아무도 끝까지 읽지 않는다.
- `◀ 최근 / n쪽 / 과거 ▶` + **[과거 기사 더 불러오기]**(네이버 검색 API `start`, 100건 단위, 1000까지).
- 네이버 뉴스와 연합뉴스(재난)를 시각순으로 합치고, 같은 링크·제목은 한 번만 센다.
- 더 가져올 것이 없으면 버튼을 없애고 마지막 쪽에 「더 이전 기사는 없습니다」.

**처리 지표는 뺀다.** 좁은 칸의 숫자 여섯 개는 아무도 읽지 않았다(§0 ②).

부서현황은 `행정조직도.md`를 근거로 만든다. 분장사무 데이터로 목록을 만들지 않는다(누락 발생).

### 6.5 조치 3버튼 — 민원 처리는 셋 중 하나로 끝난다

절차가 복잡하면 직원이 쓰지 않는다. 민원은 아래 셋 중 하나로 종결된다.

| 번호 | 버튼 | 하는 일 | 색 |
|---|---|---|---|
| **C-4a** | 현장확인 · 인력배치 | 현장에 나가는 경우. 인력 배치 + 확인 결과 기록 | 적 그라데이션 |
| **C-4b** | 용역업체 문자발송 | 업체에 넘기는 경우. **조치결과 보고 링크가 문자 본문에 함께 나간다** | 청 그라데이션 |
| **C-4c** | 조치결과 입력 | 나가지 않고 답변만으로 끝내는 경우 포함. **회신 초안은 AI 가 쓴다** | 녹 그라데이션 |

- **상세 맨 위에 가로 3열로 둔다.** 무엇부터 할지가 먼저 보여야 한다. 세부 6구획은 그 아래.
  900px 미만에서는 세로로 떨어진다.
- **직사각형 · 입체 버튼.** 긴 막대가 아니라 아이콘 + 이름 + 한 줄 설명 + 상태 배지.
  누르면 눌리는 그림자(`0 3px 0` → `0 1px 0`).
- **같은 일을 하는 버튼을 두 벌 만들지 않는다.** 인력 투입이 큰 버튼과 작은 버튼에 동시에 있으면 안 된다.
- 보고 링크를 따로 복사하게 하지 않는다. **문자 하나로 끝나야 한다.**

### 6.5-1 처리단계는 담당자가 고르지 않는다

무엇이든 조치가 시작되면 **처리중**, 조치가 끝나면 **완료**로 스스로 바뀐다.
드롭다운을 여는 것도 일이다. 화면에는 지금 어느 단계인지만 보여 준다.

| 계기 | 결과 |
|---|---|
| 용역업체 문자발송 · 조치결과 입력 · 처리방안 안내 · 부서 지정 투입 · 대응 이력 기록 | 처리중 |
| 처리 결과가 `조치 완료` · `현장 이상 없음` · `안내로 종결` | 완료 |

### 6.5-2 대응 이력은 막 적는다

상황이 움직일 때마다 한 줄씩 남긴다. **시각은 자동으로 붙는다.**
`14:35 …` `오후 2시 42분 …` 처럼 시각을 적으면 그 시각을 쓰고,
오전·오후 없는 1~7시는 낮으로 본다(당직 기록에서 새벽은 보통 ‘오전’을 붙인다).
여러 줄을 붙여 넣으면 줄마다 한 건으로 남고, 시각순으로 정렬된다. `Ctrl+Enter`.

### 6.5-3 사진은 장수에 맞춰 칸이 바뀐다

한 자리에 **최대 20장**. 2장이면 2칸, 3~6장 3칸, 7~12장 4칸, 13~20장 5칸.
넘치면 숨기지 않고 「그 밖 N장」이라고 적는다.
**사진이 있는 민원은 지도 마커가 그 사진이 된다.**

### 6.6 담당부서 · 연락처 · 담당 주체

- **부서를 고르는 곳은 전부 같은 조직도 화면을 쓴다**(F-3 · C-6b · I-4a · C-4a1b).
  **여러 곳을 이어서 고를 수 있다.** 첫 번째가 주관, 나머지는 협조 부서다.
  다시 누르면 빠지고, 변경 사유는 고른 부서 바로 아래에 둔다.
  검색은 위에 붙어 있고, 과를 누르면 그 자리에서 담기며, 저장·닫기는 오른쪽 위에 있다.
  목록만 늘어놓으면 어느 국 소속인지 모른 채 고르게 된다.
- **C-6 배정 근거는 버튼으로.** 본문에 근거를 늘어놓지 않고 눌러서 팝업으로 본다.
- **C-7 연락처는 이름과 번호를 각 줄에.** 부서명 뒤에 번호를 바로 붙이면 번호가 다음 줄로 밀린다.
- **C-11 담당 주체의 축은 `생활민원 / 교통·도로 기동단`.** ‘당직 처리’는 부서가 아니라 근무 형태였다.

---
## 7. 용역업체 조치결과 보고 `#done`

문자 인계 시 **처리요청과 함께 전용 링크**를 보낸다. 업체 직원이 현장에서 휴대폰으로 사진만 올리면 끝난다.

### 7.1 원칙

- **로그인·앱 설치 없음.** 시민 창구와 같은 방식이다.
- **텍스트 입력을 요구하지 않는다.** 바쁜 현장이다. 메모는 접어두고 원하는 사람만 펼친다.
- 위치·시각·업체·담당자는 링크와 사진에서 자동으로 채운다.

### 7.2 화면 (한 장)

```
① 민원 요약   유형 · 주소 · 요청사항 · 신고 사진   (읽기 전용)
② 사진 촬영   [조치 전] [조치 후]  ← 누르면 카메라 바로 열림
③ 결과 선택   (완료) (추가 작업 필요) (현장 이상 없음)
④ [전송]      높이 48px
   메모 (선택) ▾
```

### 7.3 현장 조건

- 회선이 나쁘면 자동 대기 후 재전송. 화면에는 `전송 중`만 표시
- 촬영 즉시 압축(장변 1600px, 500KB 이하 목표)
- 촬영 위치가 신고 지점과 멀면 안내만 하고 **막지 않는다**(현장 사정이 있을 수 있다)
- 처리 완료 후 링크 만료. 만료 시 `이미 처리된 민원입니다`

### 7.4 보안 제한

로그인이 없으므로 주소를 아는 사람은 열 수 있다. 그래서 다음으로 제한한다.

- 그 민원 **한 건만** 열린다. 다른 민원 조회 불가
- **신고인 개인정보를 표시하지 않는다.** 유형·주소·요청사항까지만
- 토큰은 민원별로 다르게 발급하고 만료 시각을 둔다

업체가 올린 결과는 민원 상세의 **조치결과 구획**에 그대로 표시된다.

---

## 8. 타이포·색·순서

| 요소 | 크기 | 굵기 |
|---|---|---|
| 화면 제목 / 지표 숫자 | 22 / 30 | 700 |
| 상세 헤더 유형명 | 17 | 700 |
| 본문·카드 유형 | 13~15 | 400~600 |
| 카드 2줄·라벨·시각 | 12 | 400 |
| 배지 | 11 | 700 |

- 줄간격 1.5, 카드 안 1.35. 숫자는 `tabular-nums`.
- **한 화면에서 6단계를 넘는 크기가 나오면 규격 위반이다.**

```css
--hs-blue:#1D4994; --hs-orange:#F07B27;
--urgent:#D63B1F; --prog:#F5A623; --done:#2B7DE0; --ok:#127A50; --plan:#7A8CA0;
간격 4·8·12·16·24·32·48   둥글기 4·8·12·16   버튼 32·40·48
z-index 0 지도 / 400 지도도구 / 600 패널 / 800 헤더 / 1000 오버레이패널 / 1400 백드롭 / 1500 모달 / 1800 토스트
```

### 8.1 z-index 는 변수로만 쓴다

값을 코드 곳곳에 숫자로 적으면 다시 어긋난다. 여덟 단계를 `:root` 변수로 선언하고 그 변수만 참조한다.

```css
:root{ --z-map:0; --z-maptool:400; --z-panel:600; --z-header:800;
       --z-overlay:1000; --z-backdrop:1400; --z-modal:1500; --z-toast:1800; --z-in:1; }
```

- `--z-in`(1)은 **구획 내부**의 겹침이다(스크롤 영역 안의 고정 머리글 등). 자기 구획 경계를 넘지 않는다.
- **Leaflet 기본 컨트롤은 z-index 800~1000 이다.** 그대로 두면 패널·헤더·모달을 덮는다.
  `.leaflet-pane` `.leaflet-top` `.leaflet-bottom` `.leaflet-control` 을 **지도 도구 단계(400)로 끌어내린다.**
- 모달이 열리면 `body.modal-on` 을 걸어 **지도 위 도구를 숨긴다.** 계층만으로는 지도 도구가 반투명 배경 위에 비쳐 보인다.
- **`.leaflet-pane` 은 덮지 않는다.** Leaflet 은 pane 마다 z-index 를 나눠 쓴다(타일 400 · 마커 600 · 팝업 700).
  한 값으로 덮으면 그 내부 순서가 평평해져 **지도 팝업이 레이어 칩과 동률이 되고 DOM 순서상 칩이 이긴다.**
  라이브러리의 계층을 사다리에 맞출 때는 **컨트롤 모서리(`.leaflet-top` `.leaflet-bottom` `.leaflet-control`)만** 건드린다.
- 지도 팝업(`.leaflet-popup-pane` 700)은 **지도 위 도구(400)보다 위**에 온다. 칩이 팝업을 가리면 내용을 읽을 수 없다.

---

## 8-1. 처리결과 `#dash` — 기간

누적만 보여 주면 「이번 주에 무슨 일이 있었나」를 알 수 없다.
**주 · 월 · 연 · 전체** 중 하나를 고르고 `‹ ›` 로 앞뒤로 넘긴다.
달력(`input[type=date]`)으로 날짜를 고르면 그 날이 든 구간을 본다. `[오늘]` 로 돌아온다.

- **화면의 모든 집계가 이 기간을 따른다** — 지표·추이·시간대·분야·관할구·행정동·이력 표.
- 기록마다 날짜를 하나 정한다(`recDate`). 옛 이력은 `7/17` 처럼 연도가 없어 올해로 본다.
- **전체**에서는 앞뒤 넘기기를 잠근다. 넘길 곳이 없다.
- 0건인 기간에도 화면이 깨지지 않아야 한다(최댓값 하한 1).

### 8-1-1. 행정동별 누적(H-11)

관할구별(H-7) 바로 아래. 위치 문구에서 동을 읽어(`emdOf`) 상위 12개를 막대로 보인다.
- **셀 수 없는 것을 숨기지 않는다.** 위치가 적히지 않은 건수와, 12개를 넘는 동 수를 함께 적는다.
  (옛 이력은 관할구까지만 남아 있어 동을 알 수 없다.)

---

## 9. 반응형

| 폭 | 배치 |
|---|---|
| 1440+ | 3열 |
| 1280~1439 | 좌 280 · 우 300, 지도 560 이상 |
| 1024~1279 | 우측을 오버레이로 |
| ~700 폰 | 지도 전체 + 하단 3버튼(민원·지도·상세) 시트. 사이드바는 시트로 전환. 접수·일지는 `PC에서 이용` 안내 |

---

## 10. 겹침·잘림 — 현재 확인된 것과 해결책

화면에서 반복해서 나타나는 문제다. **개별 위치 조정이 아니라 아래 원인별 규칙으로 해결한다.**

| # | 현상 | 원인 | 해결 |
|---|---|---|---|
| 1 | 좌측 인력현황·실시간뉴스가 잘리고 각자 스크롤바가 생김 | 좌측 한 칸에 목록·인력·뉴스 세 가지를 넣음 | **인력·뉴스를 일반현황(`#info`)으로 이동**(6.4). 좌측은 목록만 |
| 2 | 하단 `상세·접수·일지` 탭이 화면 아래를 가림 | 우측 패널이 좁아 하단으로 밀려남 | **우측 패널 4상태로 통합**(6장). 하단 탭·스트립 모두 제거 |
| 3 | 지도 우상단 도구가 레이어 칩 줄과 겹치고 일부가 잘림 | 칩과 도구가 같은 상단 영역을 씀 | 칩은 좌상단 한 줄, 도구는 **우상단에서 12px 아래로 내려** 세로 배치. 서로 침범 금지 |
| 4 | 지도 마커가 서로 겹쳐 덩어리로 보임 | 마커가 크고 장식적 | **28px 이내 단순 형태.** 가까운 건은 묶음 표시(개수 배지, 내부 최고 심각도 색) |
| 5 | 담당 연락처가 제목·본문에 같은 문장 2회 반복 | 템플릿에서 요약과 본문을 모두 출력 | 한 번만 표시. 업체명·담당 전화 한 줄로 |
| 6 | 우측 패널 내용이 아래에서 잘림 | 패널 안에서 여러 구획이 각자 스크롤 | 상세는 **한 덩어리로 스크롤**. 조치 버튼만 상단 고정 |
| 7 | 우측 상단 주황 원형 버튼이 패널 위에 떠 있음 | 정체 불명 요소 | 확인 후 제거하거나 라벨을 붙여 도구 묶음에 편입 |
| 8 | `타임라인` 버튼이 하단 탭과 겹침 | 하단에 두 체계가 공존 | 하단 영역 자체를 없앤다(2장) |
| 9 | 지도 우상단 도구의 글자가 버튼을 벗어남 | 32px 원형에 두 글자를 넣음 | **알약 모양으로**(5.1). 높이 32px, 폭 auto |
| 10 | 화면마다 헤더가 다름 | 화면별로 헤더를 따로 그림 | **공통 부품으로**(3.2-1) |
| 11 | 민원 레이어를 끄면 선택한 민원도 사라짐 | 선택 마커가 레이어 토글을 따름 | 선택 마커는 토글과 무관하게 표시(5.1) |

### 10.1 재발 방지 규칙

1. **떠 있는 요소는 자기 영역 안에만 있는다.** 지도 도구는 지도 안, 패널 버튼은 패널 안. 경계를 넘는 `position:fixed`를 쓰지 않는다.
2. **z-index는 8단계만**(8장). 그 외 값은 코드에서 제거한다. 지도 위 도구는 400을 넘지 않는다.
   숫자를 직접 적지 말고 8.1의 변수만 쓴다. **외부 라이브러리(Leaflet)의 기본 z-index 도 사다리 안으로 끌어온다.**
3. **스크롤 영역은 화면당 2개 이하.** 세 번째가 생기려 하면 그 구획의 내용을 줄이거나 다른 화면으로 옮긴다.
4. **고정 높이 안에 안 들어가면 항목을 뺀다.** 글자를 줄이거나 줄간격을 좁혀 욱여넣지 않는다.
5. 새 요소를 추가할 때 **무엇을 빼는지 함께 적는다.**

---

## 11. 검증 (1440×900 실측)

- [ ] 스크롤 영역이 **2개 이하** (좌측 목록, 우측 패널)
- [ ] 좌·우 사이드바가 접히고 접힘 상태가 저장됨
- [ ] 경계 드래그로 폭이 조절되고 지도가 480px 아래로 내려가지 않음
- [ ] 헤더 탭이 민원현황-접수·일반현황·처리결과·교통·도로 기동단 4개
- [ ] 조치 버튼이 3개이고 상세 맨 위에 가로로 나란히 있으며, 같은 일을 하는 버튼이 중복되지 않음
- [ ] 현장 결과보고에서 제출을 누르면 창이 닫히고, 빠진 칸이 있으면 그 자리에 표시됨
- [ ] 7개 화면 모든 요소에 항목번호가 있고 docs/항목번호.md 와 어긋나지 않음
- [ ] 담당 주체가 `생활민원 / 교통·도로 기동단` 2택
- [ ] 배정 근거가 버튼 → 팝업이고, 연락처의 이름·번호가 각 줄에 있음
- [ ] 좌·우 사이드바를 접어도 지도가 남고, 손잡이가 계속 보임
- [ ] 일반현황이 기상(상시) + 인력·부서 버튼 + 뉴스이고 처리 지표가 없음
- [ ] 부서현황이 나열식이 아니라 시장→부시장→실·국→과 조직도이고, 검색해도 계층이 유지됨
- [ ] 뉴스가 한 쪽 20건으로 넘어가고 [과거 기사 더 불러오기]로 이전 구간이 붙음
- [ ] 출동 인력 마커가 끊기지 않고 부드럽게 이동하고, 지나온 선이 마커와 같이 움직임
- [ ] 사진을 올리면 전송 전에도 바로 보이고, 새로고침해도 남아 있음
- [ ] 상세 구획이 반쪽으로 잘리지 않고, 우측 패널을 넓혀도 **한 단**을 유지함
- [ ] 처리단계를 손으로 고르는 자리가 없고, 조치에 따라 처리중·완료로 자동 전환됨
- [ ] 무언가 적는 중에 새 민원이 들어와도 창이 꺼지지 않고 알림만 뜸
- [ ] 대응 이력을 막 적으면 시각이 붙고, 여러 줄을 한 번에 넣을 수 있음
- [ ] 사진이 장수에 맞춰 칸이 바뀌고(최대 20장), 지도 마커에도 사진이 보임
- [ ] 분류 카드 아래 [민원제보 확인중]·[처리대상 민원목록]이 있고, 누르면 목록·지도가 함께 좁혀짐
- [ ] 접수 입구 3버튼이 각기 다른 그라데이션이고, 최근 처리가 10건까지만 보임
- [ ] 부서를 고르는 모든 자리가 조직도 화면이고 검색이 됨
- [ ] 조치지시에 근거가 된 민원이 유형별·목록으로 보이고, 눌러서 팝업으로 확인됨
- [ ] [상황판에서 크게 보기]가 그 민원 하나만 확대해 보여 줌
- [ ] [이상징후 확인]이 그 지역 민원들만 지도에 맞춰 보여 주고 [전체 보기]로 풀림
- [ ] `#road` 의 [기동단이란]이 그 화면 안에서 펼쳐지고, [전화 접수]가 접수 화면으로 이동함
- [ ] 시민창구가 위치 → 대분류 2개 → 세부분류 순서이고, 위치 권한이 켜져 있으면 안내가 뜨지 않음
- [ ] 모든 화면의 헤더가 동일하고, 시민 화면은 탭·소속이 숨겨짐
- [ ] 하단 스트립·하단 탭이 사라짐
- [ ] 좌측 상단에 분류 카드 3개(긴급·일반·교통도로)가 있고 건수가 맞음
- [ ] 좌측에 민원 카드 **10개 이상** 보임
- [ ] 민원 레이어를 꺼도 선택한 민원이 지도에 보임
- [ ] 지도 우상단 도구가 알약 모양이고 글자가 넘치지 않으며 **6개를 넘지 않음**
- [ ] 내위치가 좌상단 확대·축소 아래, 전체화면이 우하단에 있음
- [ ] 기상 관제를 켜면 레이어 칩이 [☰ 레이어] 하나로 묶이고 Windy 도구 줄과 겹치지 않음
- [ ] 행정동 경계 29개가 기본으로 보이고, 동 이름표가 읽히며, 민원 마커를 가리지 않음
- [ ] 처리결과에서 주·월·연을 고르고 넘기면 모든 집계가 그 기간으로 바뀜
- [ ] 좌측 목록 기본값이 [처리대상 민원목록]이고, 확대·축소 버튼이 칩에 가리지 않음
- [ ] 관할구별 아래에 행정동별 누적이 있고, 셀 수 없는 건수를 함께 적음
- [ ] 부서 고르기에서 여러 곳을 이어서 고를 수 있고, 조치지시 하달 이력이 급증 없이도 보임
- [ ] 지도 마커 팝업이 레이어 칩·우상단 도구에 가려지지 않음
- [ ] 우측 기본 상태에 접수 버튼 3개가 있음
- [ ] 접수 버튼을 누르면 640px로 넓어지고 지도 클릭으로 위치가 지정됨
- [ ] 민원 상세가 접힘 없이 펼쳐지고, 조치 4구획 + 세부 6구획 순서가 맞음
- [ ] 처리주체 변경이 부서 목록 선택 방식임
- [ ] 일반현황에 인력·부서현황·뉴스·기상·지표가 모두 보임
- [ ] 용역업체 링크로 사진 2장 + 결과 선택만으로 보고가 완료됨
- [ ] 검색 문구가 `민원검색`이고 **헤더 표시 필터·범위 버튼이 모두 제거됨**
- [ ] 좌측 분류 카드 3개가 그라데이션 입체 버튼이고, 고른 것만 진하게 보임
- [ ] 모든 화면 요소에 항목번호(`data-no`)가 있고 Ctrl+Shift+N 으로 보임
- [ ] 겹침 11항목이 모두 해소됨
- [ ] 모달이 열렸을 때 지도 도구·레이어 칩·Leaflet 컨트롤이 모달 위로 올라오지 않음
- [ ] `#road`에서 [기동단이란?]을 누르면 우측에 안내가 뜨고 지도·목록이 유지됨
- [ ] 안내문의 5개 유형이 접수 화면 유형 버튼과 일치함
- [ ] BUILD_VER 갱신

---

## 12. 작업 방식

1. `redesign` 브랜치. `main` 금지.
2. **접수·일지 분리를 먼저** 한다. 상황판에서 두 기능을 들어내야 나머지 배치가 성립한다.
3. 그다음 상황판 셸(헤더·좌측·지도·우측·하단)을 이 사양대로 새로 작성해 교체.
4. 기능(함수·API)은 재사용. 껍데기만 교체.
5. 커밋 전 검증 3종(JS 문법·파일 크기·핵심 앵커).
6. 완료 후 **11장 체크리스트를 항목별로 측정해 보고**한다. "적용했다"가 아니라 "카드 12개가 보인다"처럼 수치로.

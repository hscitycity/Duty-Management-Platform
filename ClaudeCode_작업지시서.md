# Claude Code 작업 지시서 — 도와드림QR 개편

> 대상 저장소: https://github.com/hscitycity/Duty-Management-Platform
> 기준 시안: `개선통합시안.html` · 규격: `UIUX_레퍼런스와_치수체계.html` · 맥락: `CLAUDE.md`
> 복구 지점: 태그 `v1-before-redesign` (개편 착수 전 안정 버전)

---

## 0. 작업 전제 — 반드시 지킬 것

### 0-1. 브랜치
`main`은 배포 중인 화면이다. **직접 수정 금지.**
```bash
git checkout -b redesign
```
작업은 전부 `redesign`에서 한다. 단계별로 완료될 때마다 사용자에게 확인받고, 승인 후에만 `main`에 합친다.
개발 도중에도 배포 사이트(`main`)는 정상 동작해야 한다.

### 0-2. 커밋 전 검증 3종 (예외 없음)
1. JS 문법 검사 — `<script>` 블록을 임시 추출해 `node --check`
2. `index.html` 크기가 정상 범위인지 (작업 D-01 이전 4~9MB, 이후 0.8~3MB)
3. 핵심 앵커 존재 확인 — `sbInit` `dlDaily` `openManual` `toggleJijeok` `citPick` `outOfCity`

### 0-3. 수정 방식
- 5MB 단일 파일이므로 **국소 치환만**. 통짜 재작성 금지.
- 치환 전 앵커가 실제로 존재하는지 `grep`으로 먼저 확인. 앵커 불일치로 패치가 조용히 누락된 사고가 2회 있었다.
- **빈 문자열 치환 절대 금지.** `replace('', X)`로 파일이 295MB로 파괴된 전례가 있다.
- 치환 실패는 조용히 넘기지 말고 명시적으로 실패시킬 것.

### 0-4. 매 수정마다
`BUILD_VER`를 갱신한다(형식 `v월일-시분`). 현재 `v0724-1310`에서 멈춰 있어 구버전 판별이 되지 않는 상태다. **저장 시 자동 갱신되도록 만드는 것이 작업 D-07에 포함된다.**

### 0-5. 보고
각 작업 완료 시 사용자에게 ①무엇을 바꿨는지 ②확인 방법(어느 화면에서 무엇을 눌러야 하는지) ③BUILD_VER를 알린다.

---

## 1. 작업 순서

네 단계로 나눈다. 단계를 건너뛰지 않는다.

| 단계 | 목표 | 작업 |
|---|---|---|
| 1단계 | 발표 시연 안정화 | D-01 D-03 D-07 D-04 |
| 2단계 | 시민 경험 완성 | S-02 S-03 D-02 D-08 |
| 3단계 | 행정 실효성 | S-06 S-12 S-11 |
| 4단계 | 지휘 계통 | S-07 S-08 S-09 S-10 S-04 |

부수 작업(D-05 D-06 D-09 D-10, S-05)은 관련 단계에 끼워 처리한다.

---

## 2. 1단계 — 발표 시연 안정화

### D-01 인라인 표본 데이터 제거 【최우선】
- **현상**: `index.html` 5.36MB 중 `JIJEOK`(2.89MB)·`BLDGF`(1.09MB)가 인라인. 각각 코드에서 1곳에서만 참조된다.
- **사실 확인**: 전역 지적은 `tiles/jijeok`·`tiles/jjlbl` 벡터타일이, 건물은 `bldg_db/`가 이미 담당한다. 두 상수는 대체된 레거시다.
- **작업**: 두 상수를 `data/jijeok_sample.json`·`data/bldg_sample.json`으로 분리하고, 사용 함수(`jijeokAt`, `bfShow`)가 필요 시점에 fetch하도록 변경. 실패 시 해당 기능만 조용히 비활성(전체 중단 금지).
- **완료 기준**: `index.html` 1.5MB 이하, 지적·건축물대장 레이어 정상 동작.

### D-03 로딩 스플래시
- **현상**: 파일을 받는 동안 흰 화면. 회선이 느린 발표장에서 멈춘 것으로 보인다.
- **작업**: 로고·슬로건이 있는 초기 화면 + 진행 표시. 지도 초기화와 DB 연결이 끝나면 사라진다.
- **완료 기준**: 새로고침 시 흰 화면 구간이 없다.

### D-07 진단 장치 정비
- `BUILD_VER` 자동 갱신 (빌드/저장 시각 기준).
- `window.onerror` 빨간 배너는 **개발 모드에서만** 표시. 평상시에는 조용한 안내로 대체.
- **완료 기준**: 발표 중 개발용 배너가 노출되지 않는다.

### D-04 시연 버튼
- **목적**: 자동 유입이 5분 주기라 발표 타이밍과 어긋난다.
- **작업**: 눈에 띄지 않는 위치(또는 단축키)에서 긴급 민원 1건 즉시 접수 → 알림 팝업 → 인력 배정 → 출동 재생까지 한 번에 진행.
- **주의**: 시연 데이터임이 이력에 남아야 한다. 실제 통계에 섞이면 안 된다.

---

## 3. 2단계 — 시민 경험 완성

### S-03 처리 현황 추적 화면 【신규·핵심】
지금 창구에는 이 화면이 없다. 접수 후 시민이 볼 것이 접수번호뿐이다.

- **처리구분에 따라 다른 화면을 보여준다.**
  - 즉시처리 — 진행 막대(접수·출동·현장확인·완료 4단 고정) + 도착 예정 **시각** + 담당 조직명·남은 거리 + 지도
  - 단기처리 — 예정일과 대기 순번, 단계 목록
  - 중장기 — 5단계 진행(교통분석·경찰협의·설계·예산·공사)
- **인력 이동 표현** (확정 사양)
  - 항상 적용: 부드러운 이동(위치 보간), 진행 방향 회전, 지나온 길 흐리게, 도착 임박 표시(잔여 18% 구간)
  - 선택: 화면 자동 추적 — 시민 화면 기본 켜짐, 상황판 기본 꺼짐. 지도를 직접 조작하면 즉시 해제되고 "다시 따라가기" 노출
  - 자동: 위치 갱신이 끊기면 마커를 흐리게 하고 "위치 확인 중" 표시 (토글 아님)
- **도착 예정**: 경로 탐색 응답의 소요시간을 사용. 남은 시간이 아니라 시각으로 표기. 1분 미만이면 "곧 도착".
- **시민에게 보낼 범위**: 담당 조직명·남은 거리·도착 예정 시각까지. **개인 이름·연락처·정확한 좌표는 전송 금지.**
- **동기화**: 당직자가 상태를 바꾸면 시민 화면 단계도 함께 넘어간다.
- **접근**: 접수번호로 재조회 가능.

### S-02 신고 흐름 재배치
- 지도 화면에서 시작하고, 지도 위 좌하단 버튼으로 신고 진입 (한 손 조작).
- 유형은 **야간 다발 5종 + 그 밖** 한 단계로. 원형 컬러 아이콘, 지름 52px 이상.
- 위치 확인은 지도 상단·조작 하단(하단 시트). 핀 고정 + 지도 이동 방식.
- "근처 신고 보기" 추가 — 중복 신고 감소, 신고 전 상황 확인.
- 접수 완료 후 **처리 현황 화면으로 바로 안내**.
- 기존 8분야 2계층은 "그 밖" 경로에서 유지.

### D-02 코드 분리 로드
- 시민 경로는 신고·추적에 필요한 것만 받는다(목표 200KB 내외). 지도 레이어 20종·매뉴얼·문서 서식·인력 정보는 당직 경로에서만 로드.
- **보안 관점**: 시민 브라우저에 당직 매뉴얼·연락처·인력 현황이 내려가지 않아야 한다.
- 주소는 하나로 유지. 화면 크기와 주소 표시(`#cit` `#duty` `#dash` `#road`)로 분기. QR은 `#cit`로 만든다.

### D-08 반응형 정비
- 상황판: PC 3열 → 태블릿 2열 → 폰은 지도 전체화면 + 하단 시트. 폰에서 문서 생성은 "PC에서 이용" 안내.
- 시민 창구는 폰 기준 설계, PC에서는 가운데 420px 고정.
- 아이폰 하단 안전 영역 처리(홈 인디케이터에 버튼이 가리지 않게).
- 홈 화면 추가(아이콘·이름·테마색)는 선택 기능으로. **앱 설치를 요구하지 않는다는 원칙은 유지.**

---

## 4. 3단계 — 행정 실효성

### S-06 AI 담당부서 지정
- **근거 데이터 3종**
  1. 행정기구 조례 별표 8 — 본청 65개 부서 분장사무 1,892건
  2. 사무위임 규칙 별표 1~3 — 구청장·읍면장·동장 재위임 사무
  3. 도로 등급 — 고속국도(도로공사, 시 소관 아님) / 국도·지방도·시도(시장) / 마을도로(읍면동)
- **배정 결과는 두 층으로 분리 표시**: `법적 소관`(조례 기준, 예: 도로과)과 `처리 주체`(실무 기준, 예: 매송면사무소).
- 배정 근거 문구를 함께 생성한다. 예: "도로과 · 조례 별표 8 「도로 유지·보수」 / 매송면사무소 · 소규모 보수 현장처리 기준".
- 확신이 낮으면 담당자 확인을 요청하고, 담당자가 바꾼 이력은 남긴다.
- **시 소관이 아닌 신고**(고속국도 등)는 접수 단계에서 안내·이관 처리.
- **입력 대기**: 마을도로 shp(사용자 제공 예정), 소규모 보수 경계 기준(미정 → 조정 가능한 값으로 구현).

### S-12 신속기동단 작업판 (별도 화면)
- 도로교통 민원만 자동으로 걸린다. 진입은 `#road`, 소속이 기동단이면 기본 화면.
- 3열 구성이되 **열마다 관리 기준이 다르다**: 즉시=남은 시간(4시간), 단기=작업 일정(7일), 중장기=진행 단계(6개월+).
- 기한 임박·초과를 가장 먼저 보이게. 임박 주황, 초과 위험 빨강 테두리.
- **작업 묶음 제안**: 같은 읍면동·같은 유형의 단기처리 건이 일정 반경 안에 쌓이면 한 번에 배정하도록 제안.
- 처리구분 판정은 사용자 제공 분류표(즉시/단기/중장기 유형 목록)를 기준으로 하고, 유형만으로 애매하면 사진·문구로 보정.

### S-11 소속 기반 범위 분리 (D-09 포함)
- 접속하면 자기 소속 범위가 이미 걸려 있어야 한다.
- 범위: 담당자=본인 처리 건 / 읍면동장=관내 전체 / 부서장=부서 소관 전건 / 기동단=도로교통 전체 / 시장·부시장·상황실=전체.
- 읍면동 화면은 탭 두 개 — "우리 면 전체"(타 부서 처리 건은 흐리게 + 부서명 배지)와 "우리가 처리한 건".
- 집계도 우리 처리·타 부서를 나눠 센다. 현재 범위 그대로 내려받기 제공.
- 중장기 진행 건은 완료 결과보고와 섞지 않는다.

---

## 5. 4단계 — 지휘 계통

### S-07 시장 조치지시
- **모든 민원이 올라가지 않는다.** AI 에이전트가 동일 지역 유사 민원 급증을 탐지했을 때만 상신.
- 상단에 탐지 근거 표시(건수, 시간, 반경, 평소 대비 배수, 특보 상태) — HUD 패널.
- 조치 유형 3분류: **일반민원 / 긴급민원 / 선제조치**. 각 카드에 해당 유형 총원이 미리 계산돼 있다.
- 부서별 인원은 **증감 버튼(40px 정사각) 상시 노출**. 슬라이더 금지, 조정 단계를 감추지 않는다.
- 총원 경고 양방향: 과다(비번 소집·초과근무), 과소(대응 지연).
- 하달 후 부서별 수신확인이 시각과 함께 표시되고, 미수신은 자동 재통보.
- 용어는 **조치지시**. 지휘결심 등 군 용어 사용 금지.

### S-08 급증 탐지 규칙 설정
- 규칙을 코드에 박지 말고 **데이터로 두고 화면에서 조정**한다. 본 서비스에서는 정책협의체 심의로 결정한다.
- 항목: 반경 / 시간창 / 최소 건수 / 평소 대비 배수 / 시장 상신 건수 / 가중(특보)·억제(재발동)·예외(행사).
- **개정 전 과거 데이터 검증**: 바꾼 값을 누적 민원에 적용해 "최근 30일이면 몇 회 발동"을 계산해 보여준다. 판정 문구도 함께(잦으면 경보 무시 우려, 드물면 놓칠 위험).
- 상태: 초안 → 심의 상정 → 의결 → 시행(시행일 지정). 초안은 실제 동작에 영향 없음.
- 개정 이력: 누가·언제·무엇을·심의번호.
- 탐지와 상신 임계치를 분리한다(예: 3건 부서 알림, 6건 시장 상신).

### S-09 보류 기록·재상신
- **사유 없이 보류 불가.** 선택형으로 둔다: 담당부서 자체 처리 / 이미 조치 중 / 현장 확인 필요 / 기상 호전 예상 / 가용 인력 부족 / **오탐**.
- 오탐 선택은 탐지 규칙 개정 자료로 자동 전달.
- 기록 항목: 상신 근거, 보류자·시각·사유·메모, 재검토 시점, 경과(추가 접수 건수), 종결 사유.
- 자동 재상신 조건: 추가 신고 증가 / 시간 경과 / 특보 상승 / 인접 확산 / 미처리 누적 / **인명 관련 신고 접수 시 즉시(억제 무시, 해제 불가)**.
- 재상신 2회 미결 시 부시장 병행 통보.
- 보류 건은 일일보고에 자동 반영.

### S-10 현장 결과보고
- 자동 입력: 민원번호·위치·도착시각·담당자·소요시간. 직원이 넣는 것은 사진과 조치 결과뿐.
- 사진은 **조치 전·후 2장**. 촬영 위치가 신고 지점과 멀면 경고.
- 조치 결과 4종: 조치 완료(조치 후 사진 필수) / 임시 조치(재방문 일정 자동 등록, 신고는 열린 상태 유지) / 타 부서 이관(소관 부서 알림·이력 기록) / 현장 이상 없음.
- 사진 공개 여부 선택. 사람 얼굴·차량번호 포함 시 비공개 안내.
- 통신 실패 시 기기에 저장했다가 자동 재전송.
- 제출 시 **시민 화면 완료 전환 + 일일보고 반영 + 처리시간 집계**가 한 번에 이뤄진다.

### S-04 중장기 진행 공개
- 5단계 고정: 교통분석 → 경찰 협의 → 설계 → 예산 → 공사.
- 신호등·횡단보도·단속장비는 **경찰 심의 단계를 자동 삽입**하고 일정·결과를 기록.
- 단계 전환 시 자동 통지. 변화가 없어도 월 1회 현황 안내.
- 타당성 미달로 종결 시 사유와 근거를 함께 안내.

---

## 6. 부수 작업

| 코드 | 내용 |
|---|---|
| D-05 | 레이어 지연 로딩 — 버튼을 누른 시점에만 해당 레이어 로드 |
| D-06 | 핀 묶음 표시(클러스터) — 누적 증가 대비 |
| D-10 | 처리시간·상습지점 집계 신설 — 대시보드에 평균 처리시간 지표, 누적 지도에 밀도 표현 |
| S-05 | 상황판 치수 정비 — 레이어 버튼 높이 36px·여백 12px 통일, 카드 여백 16/24px, 지표 위계 30/12px |

---

## 7. 공통 규격 (모든 화면 적용)

- **기준**: KRDS(행정안전부 정부 디자인 시스템) 구조 + 화성시 색 + `design.md` HUD 문법
- **색**: 기본 `#1D4994` / 강조 `#F07B27` / HUD `#38bdf8` / 완료 `#2B7DE0` / 진행 `#F5A623` / 위험 `#D63B1F`
- **간격**: 4·8·12·16·24·32·48 일곱 값만. 7·9·13·18px 같은 임의값 금지
- **글자**: 12·13·15·17·22·30px 여섯 단계. Pretendard(본문) + Paperlogy(대형 제목만)
- **버튼**: 32(보조)·40(기본)·48px(주요·모바일). 모바일 터치 대상 48px 이상
- **둥글기**: 4(배지) 8(카드·입력) 12(모바일 버튼) 16(하단 시트)
- **HUD와 라이트 구분**: 경보·상황 인지는 HUD(네이비+사이언 글로우), 입력·기록은 라이트 카드
- **접근성**: 아이콘 전용 버튼 `aria-label`, 포커스 링 제거 금지, `prefers-reduced-motion` 대응

---

## 8. 참고 파일

| 파일 | 용도 |
|---|---|
| `개선통합시안.html` | 목표 화면 13절. 이 문서의 시각적 근거 |
| `UIUX_레퍼런스와_치수체계.html` | 치수·색 기준과 레퍼런스 10선 |
| `설계인스펙터.html` | 현재 화면 위에 개발 요소 34개 표시. 수정 지점 지목용 |
| `시스템_설계도.html` | 현 시스템 구조 트리(화면·레이어·데이터·자동화) |
| `인력이동_표현_레퍼런스.html` | 이동 표현 기법별 구현 방법 |
| `CLAUDE.md` | 저장소 구조·키·데이터 흐름·사고 이력 |

---

## 9. 사용자 입력 대기 항목

작업 중 아래가 필요하면 진행을 멈추고 요청할 것.

1. 마을도로 shp — S-06 도로 등급 판정용
2. 소규모 보수 읍면동 처리 기준 — 없으면 조정 가능한 값으로 구현 후 확인
3. 심평원 약국 API 승인 여부
4. 히어로 배경 이미지 — 있으면 적용
5. 연합뉴스 수집기 가동 확인 (등록 IP 사무실 회선 전용)

---

## 10. 하지 말 것

- `main` 직접 수정
- 통짜 재작성
- 슬라이더로 인원 조정 (증감 버튼만)
- 별점·후기·제보 취소 투표 등 민간 평가 장치 도입
- 시민 화면에 담당자 개인정보·정확한 좌표 전송
- 로드맵 항목(알림톡·통화 STT·행정망 이관) 임의 구현 — 문서화만
- 지시서에 없는 기능 추가. 필요하면 먼저 제안하고 승인받을 것
